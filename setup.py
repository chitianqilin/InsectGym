from setuptools import setup

setup(name='InsectGym',
      version='0.0.1',
      install_requires=['gym',
                        'numpy',
                        'opencv-python',
                        'matplotlib',
                        'gym',
                        'scipy'
                        ]  # And any other dependencies Env needs
      )
